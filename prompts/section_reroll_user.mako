Target date: ${today_iso}.
Section key: ${key}
Section title (informational only): ${title}

Topic: ${topic}

Run multiple web_search queries until you have at least ${str(target)} fresh, distinct items.
